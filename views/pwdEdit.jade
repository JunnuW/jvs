extends layout

block content
  form(method='POST' role="form")
    legend Change your Rock.Phys. password
    .form-group
      p You are logged in as: #{user.username}
      label(for='EmailAddr') Your email:
      input.form-control(type='text', name='emailAddr', value=user.email, autofocus)
    .form-group
      label(for='NewPassword') New password
      input.form-control(type='password', name='NewPassword')
      label(for='RepNewPwd') Confirm new pwd
      input.form-control(type='password', name='RepNewPwd')
    button.btn.btn-primary(type='submit') save changes
    a.btn.btn-link(href='/') Cancel changes
